# Install the runtime and model dependencies into the image in one pass. The
# additional index supplies the CUDA 12.8 PyTorch wheels used on Blackwell.
--extra-index-url https://download.pytorch.org/whl/cu128

reactor-runtime==3.1.2

# B200-compatible replacement for AlayaWorld's upstream PyTorch 2.6/cu124
# stack.
torch==2.9.1
torchvision==0.24.1
torchaudio==2.9.1

# Reactor Runtime requires NumPy 2.x. AlayaWorld v1.1 and ViGeo share this
# version in the serving image.
numpy>=2.1,<3
scipy>=1.14
safetensors>=0.4
einops>=0.7
diffusers==0.39.0
transformers==5.13.1
peft>=0.17
bitsandbytes>=0.45
opencv-python-headless>=4.10
pillow>=10.1
pyyaml>=6.0.3
tqdm
easydict
huggingface-hub

# The public AlayaWorld modules import these media and configuration packages
# while assembling their inference surface.
addict
imageio
matplotlib
moviepy==1.0.3
omegaconf
plyfile
# The v1.1 trainer imports its video dataset module while loading inference.
decord==0.6.0
