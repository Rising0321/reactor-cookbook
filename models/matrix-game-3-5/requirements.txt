# Python dependencies installed into the generated serving image in the same
# resolution as Reactor Runtime. `build.runtime_version` in reactor.yaml owns
# the Runtime version; Matrix's upstream packages remain isolated in the
# Python 3.10 worker environment prepared at model load.

# Image validation and normalization in the serving process.
Pillow>=10.1
